export default function Dashboard() {
  // const { user, currentUser, authTokens } = useAuthContext();

  // console.log(authTokens, Date.now());
  // console.log(user?.exp, user?.iat, user?.iat > Date.now());

  return (
    <div>Dashboard</div>
    // <div>
    //   {!user
    //     ? 'No user available'
    //     : `Welcome  ${currentUser?.full_name} to your new dashboard with user email:${currentUser?.email}`}
    // </div>
  );
}
